fastapi==0.109.2
uvicorn[standard]==0.27.1
celery==5.3.6
redis==5.0.1
asyncpg==0.29.0
openai==1.30.5
pymupdf==1.23.8
pandas==2.1.4
numpy==1.26.3
httpx==0.26.0
beautifulsoup4==4.12.3
# lxml: fast XML/HTML parser used by the SEC EDGAR client (Week 3 / Day 1)
# and any future BeautifulSoup-based parsing that wants the lxml backend
# instead of html.parser. Picked up by bs4 automatically when installed.
lxml==5.2.1
# weasyprint pins are coupled — the v62.x line was built against
# pydyf <0.11 (the v63.x line moved to 0.11+). Don't bump pydyf
# above 0.10.x without also bumping weasyprint, or the W10/D4
# PDF render crashes with "PDF.__init__() takes 1 positional argument
# but 3 were given" at runtime.
weasyprint==62.3
pydyf==0.10.0
# python-pptx bumped 0.6.23 -> 1.0.2 for W11/D1 deck exporter.
# 1.x line is the maintained release; 0.6.23 was 4 years stale and
# the deck-builder module uses APIs (e.g. ``Inches`` units helpers,
# ``slide.placeholders`` iteration) that work the same way on both
# but the bump unblocks any future builder that wants newer features.
python-pptx==1.0.2
python-docx==1.1.2
pydantic==2.7.4
python-multipart==0.0.6
python-dotenv==1.0.1
slowapi==0.1.9
PyYAML==6.0.1
Jinja2==3.1.3
pytest==7.4.4
pytest-asyncio==0.23.4
passlib==1.7.4
bcrypt==4.0.1
itsdangerous==2.1.2
email-validator==2.1.1
boto3==1.34.34
# litellm: hard-pinned for Phase 1 cross-family routing — its provider-prefix and
# acompletion API moves between minor versions. Do not bump without re-running
# backend/tests/test_multi_provider_smoke.py against all three providers.
litellm==1.40.20
instructor==1.3.4

# Week 2 / Day 1 — DeBERTa NLI cross-encoder runs in a dedicated Celery worker
# (see docker-compose.yml `nli_worker`). CPU-only torch is fine — we score short
# (premise, hypothesis) pairs in batches; GPU is unnecessary at this scale.
# torch is sourced from PyTorch's CPU wheel index so we don't pull the +cu126
# build by accident.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.0+cpu
sentence-transformers==3.0.1

# Week 2 / Day 2 — spaCy entity extractor for the lexical-overlap signal
# (precision-style ORG/GPE/PRODUCT/LAW/NORP detection). Lives in the main
# worker, NOT nli_worker — DeBERTa is heavy and lexical overlap doesn't
# need it. The en_core_web_sm model (~50MB) is pulled in the Dockerfile.
spacy==3.7.5
